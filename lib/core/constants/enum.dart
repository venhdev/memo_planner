enum EditType { unknown, add, edit, editInstance }

enum BlocStatus { initial, loading, loaded, success, error }

enum GroupType { myday, today, scheduled, all, done, assign }

// Task
enum MenuItem { rename, delete, hide }

enum TaskSortOptions { name, dueDate, createdDate, priority, none }
